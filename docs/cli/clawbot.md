---
summary: "CLI reference for `klawty clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `klawty clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `klawty clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `klawty clawbot qr` (same behavior as [`klawty qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `klawty clawbot qr` -> `klawty qr`
